# PharmaSynth — Remaining-Work Checklist (THE single tracker)

Everything still to do before the **2026-08-31** turnover, consolidated **2026-07-09** after the W5.6 client-workflow batch (suite **385/385** green). Check items off as they land. `[ ]` open · **bold** = Tier-1 contract-critical (Tutorial + Prelims + Benzoic Acid + Aspirin). Companion docs: [gameplay-flow.md](gameplay-flow.md) + [experiments-reference.md](experiments-reference.md) + [systems-reference.md](systems-reference.md) (the system references), [on-device-test-plan.md](on-device-test-plan.md), [client-signoff-request.md](client-signoff-request.md), CLAUDE.md (session bootstrap).

**DONE and out of scope of this list** (see CLAUDE.md for detail): engine + scoring/BKT + progression persistence, all 11 experiments as data + auto-built stages + sim-rig verbs (17/42 stations), pour path + depletion (finite bottles, restart prompt), door-gated campaign flow (choice panel → episode → lab-coat → armed load → walk-in timer → debrief → teleport return → unlock announce), screen-locked HUD + Pharmee dialogue bar + only-while-speaking bubble, ScreenFader, in-lab Settings/Restart/Quit, MainMenu spawn room, real-size pass (RealSizes, 42 prefabs), labcoat PPE in the locker (click to don), swinging lab door (solid open + closed), head-collision pushback, 44 cutscene SOs wired, 33-MCQ post-lab quiz + yield stepper, CC0 audio wired (beeps/UI/glass/alarm/music), hub/select UI, results CSV backend.

---

## 1. Per-experiment content wiring

- [x] **Product-seeded test vessels per experiment** — DONE 2026-07-09: 8 layouts got a `TestTube_WithLiquid` vessel seeded with the product (Aspirin→salicylic for the FeCl3 phenol test, Benzoic, Acetanilide, Benzamide, Chloroform, Acetone, Ethanol, Wine→CO2 for limewater) and the `test-*` pour bindings moved onto it; Benzamide also gained the missing NaOH + NaNO2 reagent vials for its alkali/nitrous tests. `TestVesselSuite` (38 assertions) pins each seeded vessel, its bindings, AND that `MasterReactionRegistry.FindReaction(seed, reagent)` fires; plus every chemical any layout references must resolve through `SceneAssetLibrary`. *Exceptions: Methane = hand-built stage (own rig); ChemicalCompounding already pours ethanol into the test beaker first (functional as-is); Caffeine has no test rules yet (murexide — see next bullet). BenzoicAcid `test-ester` + Benzamide `test-acid` pours land on the seeded vessel but await their rules (next bullet).*
- [x] Remaining reaction rules — DONE 2026-07-10: added `Test_AcetoneIodoform` (yellow ppt), `Test_BenzoateEster` (ester odour), `Test_BenzamideAcid` (hydrolysis ppt), and now **`Test_AcetoneSchiff`** (negative — ketone, `Chem_SchiffReagent` + a pourable Schiff vial + a seeded-tube binding on Layout_Acetone) and **`Test_CaffeineMurexide`** (positive purple — `Chem_Caffeine` seeded product + `Chem_MurexideReagent` + `Chem_Murexide` result + a new seeded product tube on Layout_Caffeine). Registry now **22 rules**, all verified firing via `FindReaction`; library resolves all four new chemicals; `TestVesselSuite` extended (Acetone +Schiff, new Caffeine case). *Melting-point stays a station/placement task (not a pour reaction) — no rule needed.*
- [x] **Methane verb polish** — DONE 2026-07-12 (W5.8): mortar grind is a real verb (`GrindController` on the scene mortar, `prepare-mixture` dual-path with the legacy zone-touch); the splint flame test is INTERACTIVE (bring a lit match to the filled tube — `SplintShouldFire`, 20 s auto-fallback so the tutorial never stalls); flame/bubble VFX were already in via StationVfx/FlamePop.
- [x] Wine bespoke rubric — RESOLVED 2026-07-09 (client): keep the **standard 6-category rubric** for consistency with the other experiments; no bespoke wine categories. No code change needed.
- [ ] Per-experiment ILO cards ×11 — **ILO TEXT UNBLOCKED 2026-07-10** (manuscript extracted): the official Appendix C objectives for the 8 covered experiments are transcribed in [manuscript-reconciliation.md](manuscript-reconciliation.md) §2 (Methane/Aspirin/Caffeine are game-authored — not in the manuscript, need client confirm). *Remaining: the card **art** (intro-cutscene visuals) — a production task, now no longer copy-blocked.*
- [ ] Data-sheet expected-yield ranges per experiment (display/reference only). **REFERENCE DERIVED 2026-07-10:** theoretical + typical yields computed from the Appendix C starting quantities in [manuscript-reconciliation.md](manuscript-reconciliation.md) §5 (Acetanilide ~2.96 g th., Benzamide ~1.04 g, Acetone ~5 g, Chloroform ~24.7 g, Ethanol ~6.4 g; Aspirin/Caffeine/Methane are game-authored). Yield stays **record-only, never graded**. *Remaining (optional): wire an on-screen "expected yield" hint on the data sheet.* **Yield-in-grade RESOLVED 2026-07-09 (client): yield is a lab-record entry ONLY — it never feeds the grade** (not a manuscript grading category; quiz MCQs stay the sole Documentation driver).
- [ ] `HazardZone` placement — HOT SURFACES DONE 2026-07-09: every Heat station auto-spawns a player-only `HazardZone` (rig-child test, armed only above 50 °C so placing the vessel never triggers it; `HazardousAction` mistake). *Spill-puddle zones remain optional polish. `WeighingScaleController` wiring DONE 2026-07-12 (W5.8): `WeighStation` + Balance fixture at Weigh stations; Aspirin weigh-salicylic completes ON the scale (pour binding `completesTask:false`), Acetone weigh-acetates = scoopula on the pan; live grams display.*

## 2. Physics & interaction pass

- [x] **QoL batch (user 2026-07-10 pt 3):** (a) **stools grabbable** — all 7 got convex collider + Rigidbody (kinematic-parked, mass 6) + `XRGrabInteractable` (dynamic attach) + `GrabPhysicsPolicy`, so they can be dragged in VR (NOTE: play-mode arrangements revert on exit — for permanent placement move them in edit mode, or tell Claude the spots, then run Re-Home); (b) **spawn burst = 3 waves** — `SpawnVfxBuilder` now emits 90-particle bursts at t=0/0.4/0.8 (duration 2 s), rebuilt in BOTH scenes; (c) **mirror avatar orientation fixed** — the head bone was driven with the RAW HMD rotation but Tripo bones aim +Y along the bone, twisting the head 180° ("facing the back"); `PlayerAvatarRig` now applies only the HMD's DELTA from body-forward onto the bone's authored pose, and the arm IK is position-only (raw controller rotations twisted the wrists "behind") — body-yaw math itself verified correct via edit-mode reflection dumps; (d) **every editor Play boots the cube room** — `PlayFromMenuBootstrap` sets `playModeStartScene = MainMenu` (toggle: Tools ▸ PharmaSynth ▸ Play Starts In Cube Room).
- [x] **Layout tidy-up batch (user 2026-07-10 pt 2):** (a) **sink end-tables aligned** — both `WashTable`s re-centred onto their island axes (x → ±(1.56/−1.59), were off by ~0.25); (b) **stools tucked** — the two centre-aisle stools relocated to the back desks and all seven pulled flush against their cabinet faces (no clipping, aisles clear — DevCapture-verified); (c) **glass tube unburied** — `Prop_glass-tube` was sunk INSIDE the fume-hood counter (collider fully below the deck + swallowed by `Col_Cabinet`, hence ungrabbable) → lifted onto the deck clear of the left pillar; (d) **Tools ▸ PharmaSynth ▸ Re-Home Scene Items (Adopt Current)** — NEW re-runnable menu that adopts every `DropRespawn` item's current transform as its home; run it after ANY manual re-arrangement or props snap back to stale homes after ~25 s idle.

- [x] **Physics-attributes / resting-pose audit for ALL items** (user request 2026-07-09, task #78) — DONE 2026-07-09: `PhysicsProfiles` table (42 items: mass + resting pose, companion to `RealSizes`), `GrabPhysicsPolicy` (kinematic-on-shelf → dynamic-on-release), builder applies profiles + rest-pose rotation; concave MeshColliders convexified (PhysX rejects them on dynamic bodies — 5 items fell through the world before the fix); degenerate flat-tool colliders padded. Verified: **Tools ▸ PharmaSynth ▸ Physics Audit (Drop Test)** 42/42 settle plausibly, **(Report)** clean (both re-runnable; + **(Fix Scene Items)** applied & scene saved). Suite 385 → 412.
- [x] XRI **sockets** at stations — DONE 2026-07-09: builder spawns an `XRSocketInteractor` per station pad with a `StationSocketFilter` (accepts ONLY the station's required item — wrong props bounce off; all 42 prefabs verified to carry `XRGrabInteractable`). **Drop respawn** (kill-Z + idle return-to-home) also DONE: `DropRespawn` on every spawned prop. *Still open: sockets on test-tube RACK holes (needs per-mesh anchor authoring — do with the art pass).*
- [x] **Spill & breakage mishandling penalties** (user request 2026-07-09) — CORE DONE same day: `Mishandling` policy table (20 breakable glass/porcelain prefabs; metal/wood/plastic never break; break ≥2.8 m/s ≈ 0.4 m drop; spill = un-held + tipped >60° + has liquid), `BreakableGlassware` reworked onto the modern runner (dynamic-only, shatter SFX key, `DroppedGlassware` mistake → Sanitation rubric, replacement via `DropRespawn.GoHome`), new `SpillMistake` (one `SpilledReagent` mistake per episode, re-arms when righted; the lost ml already drains via `LiquidPourer` → starvation → restart path), grader counts both against Sanitation. Builder auto-attaches to spawned glass/pourables. 14 assertions (`MishandlingSuite`). *Still open (art/VFX bucket): puddle decal + cleanup wipe task, shatter VFX; client may veto free replacements in exam periods (sign-off list).*
- [x] Teleport anchors at each workstation — DONE 2026-07-09: builder spawns a floor `TeleportationAnchor` pad 0.75 m in front of every station (mirrors the floor area's layers/provider); 2 assertions.
- [ ] Refine crude convex hulls on tall apparatus (tripod, retort stand, burner).
- [ ] XRI interaction-layer audit (everything on default layers; sockets/hands need masks).
- [x] Prop readability — DONE 2026-07-10 (UX affordance batch): `HoverHighlight` on every grabbable (58 in-scene + auto-added to spawned props by `ExperimentSceneBuilder`) brightens the item (base-colour tint via MaterialPropertyBlock — no shader/keyword dependency) and pops it 1.06× when a hand/ray hovers, restoring on exit/grab; pure `HighlightScale` + `SetHighlight` tested. Wired by **Tools ▸ PharmaSynth ▸ Wire VR Affordances**. *Final size/tint feel wants an in-headset look.*
- [x] **VR affordance batch** (user 2026-07-10) — DONE: (a) **Haptics** — `Wire VR Affordances` gives every hand interactor (NearFar + Poke) a `HapticImpulsePlayer` + `SimpleHapticFeedback` so grab / socket-snap / UI-poke buzz (select amp 0.5/0.1 s, hover 0.15/0.05 s); interactor-side, so it covers all grabbables incl. runtime props. (b) **Socket ghost preview** — station `XRSocketInteractor`s get `showInteractableHoverMeshes` + a translucent-cyan ghost material (persisted `SocketGhost.mat` for scene sockets; a runtime shared material in `ExperimentSceneBuilder` for spawned ones) so bringing the correct item near shows where it snaps. (c) **Wayfinding VERIFIED** already-wired — `WaypointGuide`(runner+marker)+`WaypointBeacon` on `ExperimentSystems` move a `WaypointMarker` (arrow+glow, hidden until a step is available) to the current step's station via `ExperimentStationRegistry`. *Haptics + ghost render only verify on-device / in Play.*

## 3. Art & models

- [ ] **Pharmee animation set** (idle-float/talk/gesture/celebrate/warn). **Face-state EXPRESSIONS DONE 2026-07-10 (user):** `PharmeeFace` rewritten (multi-renderer, MaterialPropertyBlock — no material leaks, edit-safe) and re-pointed at the robot's **Eyes + Mouth meshes** (was `Ears_Black_Matt_0`); **default = HAPPY** (green glow) including while following the player; per-line expressions from `PharmeeBrain` (happy greet/celebrate, orange warning on mistakes) AND now the gate conversation (`PharmeeMood.ExpressionForGate` — friendly at the door, warning on supply trouble/threshold, celebratory on debrief); `PharmeeMood` resets the face to happy when each line ends. Wired via **Tools ▸ PharmaSynth ▸ Wire NPC Polish**. 4 assertions.
- [ ] **Dr. Jimenez** — MODEL+RIG+ANIMATIONS DONE 2026-07-10: Tripo model (4.8k tris, stylized scientist w/ glasses+badge) placed at his stand-in (2.9, -1.1) at 1.75 m; rig = `RiggedDrjimenez.prefab` (user re-ran Tripo Rigging v1 in the AI panel after my first in-place rig retargeted horribly); humanoid Avatar built with **enforced T-pose calibration** (`DrJimenez-AvatarT.asset` — the first A-pose-calibrated avatar mangled his arms; capture-previewed BEFORE placing this time); idle/talk/walk clips generated (uthana text-to-motion; idle+talk made stationary, all looped); `Jimenez.controller` wired on the scene instance — Idle default, **Talk via the "Talking" bool**, Walk state ready. **`ExaminerNPC` voice DONE 2026-07-10:** he proctors every run aloud — a stern greeting on start then periodic oversight remarks (never hints; `PharmeeLines.ExamGreeting`/`ExamRemarks`), each driving the animator's **"Talking" bool** for its duration (`Wire NPC Polish` now guarantees the Talking param + Idle⇄Talk transitions via `EnsureTalkParam`); the `State`/`IsObserving` assessment machine is unchanged. **Visible subtitle DONE 2026-07-10:** `Wire NPC Polish` now builds a world-space overhead **`JimenezSubtitles`** bubble on Dr. Jimenez (dark panel + "Dr. Jimenez" name plate + wrapped subtitle, `FaceCamera`, sortingOrder 29000, shown only while he speaks via its own `NPCNarrationController` — mirrors Pharmee's bubble) and wires it into `ExaminerNPC`; DevCapture-verified rendering his remark above his head. *Client may still swap the model.* **Proctor roaming DONE 2026-07-10:** `ProctorRoamModel` (pure: AtHome→WalkingOut→Observing→WalkingHome, seeded idle jitter, round-robin points, quiz forces him home) + `ProctorRoamer` driver on DrJimenez (walks 0.7 m/s, faces the walk, drives the animator's "Walking" bool, watches the player half the time while observing, returns home from `ExperimentFinished` until the next start) + 4 `ProctorPoints` (reagent shelf / dynamic stage / equipment shelf / fume hood) wired by **Tools ▸ PharmaSynth ▸ Wire NPC Polish**. 9 assertions. **Pt 2 (user): CharacterController on DrJimenez** (bounds-fitted capsule, grounded at his feet — walls/furniture stop his roam via `cc.Move`, and the player can't walk through him) and the animator's **Idle⇄Walk transitions verified/wired** on the "Walking" bool (`Wire NPC Polish` ensures param + states + both transitions). **Pt 3 (user: "he runs through the wall endlessly"): never-escape failsafes** — (a) stuck watchdog (`ProctorRoamer.StuckTick`, pure+tested): 2 s without progress = treat as arrived → observe-from-here → walk home, and if stuck on the way HOME he teleports to his post; (b) 9 m leash: beyond it he snaps home instantly, whatever went wrong (missing wall collider, physics shove). *Runtime look pending a play-test.*
- [x] Real **fume hood** model — DONE 2026-07-09: Tripo P1 generation (user ran it in the AI panel — MCP can't pass instance IDs, they lose precision in JSON), 4.4k tris, glTF PBR. Placed in `FumeHood_StandIn` at 2.35 m real height, seated on the floor, sash facing the room (DevCapture-verified); glass-slab stand-in visual hidden, `FumeHoodZone`/`WorkVolume` trigger untouched, 5 solid box colliders (cabinet/pillars/top/back — sash opening stays passable for props). *Optional polish later: clean the smudged top band via Tripo re-texture.*
- [x] **Clean reagent-label textures** — DONE 2026-07-09: style locked to **MODERN** (user pick; apothecary base kept as an alternate). **Tools ▸ PharmaSynth ▸ Generate Reagent Labels** (`Editor/LabelForge.cs`, idempotent) composites each chemical's name as crisp TMP text onto `LabelBase_Modern` (never AI typography), builds a material per label, and mounts a `NameLabel` quad on each of the 16 shelf bottles facing the aisle — verified in-scene. Re-run the menu after renaming chemicals or adding bottles. *Optional later: same treatment for experiment-spawned pourables via the builder.*
- [x] **First-person mirror avatar** (user 2026-07-10) — DONE: Tripo rigged `PlayerAvatar.prefab` (casual shirt/pants/sneakers, T-pose) built into the rig via **Tools ▸ PharmaSynth ▸ Build Player Avatar**: normalized to 1.7 m, foot-offset lift (Tripo pivots at model centre — root lifted 0.854 so feet meet the floor, offset fed to the runtime driver), `PlayerAvatar` layer 8 (main camera culls it, mirror camera renders it → **mirror-only**), Animation-Rigging IK (two-bone arms → controllers, head rotation → HMD) driven by `PlayerAvatarRig` (foot-follow + smoothed body yaw). Verified standing on the corridor floor via temp-camera render. *Play-mode IK confirm pending human test.*
- [x] PPE completion — WEARABLES DONE 2026-07-10: `PPESetModel` (pure per-piece state) + rewritten `PPEController` (per-piece `Don(piece)`, `PPEWorn` = ALL THREE worn, per-piece visuals arrays) + `PPEDonOnSelect` forwarders; **Tools ▸ PharmaSynth ▸ Build PPE Wearables** makes coat/goggles/gloves individually clickable in the locker (old don-everything persistent call cleared, host `donOnSelect` off), clones worn visuals onto the mirror avatar's bones (coat→Spine01, goggles→eyes, gloves→hand bones — PlayerAvatar layer, mirror-only) AND first-person gloves onto the controllers (main-camera visible, per user: only gloves show on screen). The gate's CoatPrompt now requires ALL THREE (Pharmee names what's still missing). Render-verified fit. *Remaining: watch orientation tune on-device; a FITTED coat mesh gen is optional polish.*
- [x] **Dressing mirror at the PPE corner** (user 2026-07-10) — DONE + FIXED (user reported "still black" 2026-07-10): rewrote `MirrorPlane` to the **canonical planar reflection** (reflection-matrix + oblique near-clip pinned to the glass + inverted culling), driven by a manual `mirrorCam.Render()` each frame. THREE real root causes, all fixed: (1) the `Frame` panel's front face was **coincident with the glass and fully occluded it** — that was the "black mirror"; pushed the glass 2 cm proud (local z −0.02); (2) URP does **not** auto-render an enabled off-screen camera → RT stayed at clear-colour; now rendered by hand (same path DevCapture uses); (3) the old off-axis projection framed only skybox. **Verified in edit mode** by rendering the reflection to a RT and photographing the mirror from the player's eye — it now shows the room (lockers/floor/lab window/Dr. Jimenez). Runtime RT 512x768, distance-gated <6 m, HUD (layer 5) culled, `_BaseMap` bound explicitly. *Caveat: a mirror can only reflect existing geometry — the player sees the SURROUNDINGS now; seeing THEMSELVES additionally needs the visible body/PPE avatar (see PPE completion below). Still worth a Play-mode confirm.*
- [x] **Locker interior see-through + coat not hung** (user 2026-07-10) — FIXED: (a) see-through — double-sided (`_Cull`=0 + doubleSidedGI) the `locker room` shell material and all `LockerLiners` panels, a catch-all so no viewing angle shows through the single-sided ChemLab walls; (b) coat — raised `LabCoatDisplay` so its collar/hanger reaches the top (hem clears the mid-shelf, sleeves drape down) and added a slim metal **`CoatRail`** across the open compartment so it visibly hangs from something. DevCapture-verified head-on.
- [ ] **Wrist-watch = THE procedures display** (user headline feature, re-confirmed 2026-07-10): look-at-watch gesture (wrist supination + glance — detection already in `WristWatchController`) summons a **large, readable HOLOGRAPHIC panel** (sci-fi style) floating in front of the player listing the experiment's full checklist/procedures with done/pending states — not just the current compact step/progress summary. Plus: real watch 3D model on the wrist (primitive canvas today) + gesture tuning on-device.
- [x] **Pharmee movement realism** (user 2026-07-10) — DONE: `PharmeeAttitude` — (a) FaceCamera vertical-forward fallback makes him actually face the player; (b) body LEANS into the movement direction (`LeanFor`, head kept level); (c) hover waves/rings pulse at his base, now **30× faster** per user (`waveSpeedMultiplier=30`); (d) talk-time bob/nod while speaking. Composes with FloatBob + FaceCamera by twisting only the body child. **Give-way DONE 2026-07-10:** `PharmeeGiveWay` (pure `SideStep` + driver) steps him laterally out of the player's path when they walk into him (inside a 0.95 m bubble, pushed to whichever side he's already on, eased in/out via a new additive `FloatBob.SetGiveWayOffset` so it layers on top of follow + bob), then drifts back once they pass — wired by `Wire NPC Polish`. 3 assertions.
- [x] **Entrance gaps** (user 2026-07-10) — DONE same day: **Tools ▸ PharmaSynth ▸ Seal Entrance Gaps** (`Editor/EntranceSealBuilder.cs`, idempotent) wraps BOTH doorways (front corridor `Door` + interior lab `Door (1)`) with opaque trim strips (lintel + two jambs) centred on the wall plane and standing proud on both faces, reusing GapSealWall's dark material — covers the frame↔wall seams from either side. DevCapture-verified at both doors: seams gone, the only sky now shows through the intentional door windows. (The lab door derives its footprint from `DoorFrame_1`; the frameless front door falls back to the leaf bounds.)
- [ ] **Reagent label aesthetics redo** (user 2026-07-10): current flat quads read as pasted nameplates and overpower thin glassware. Replace with size-aware labels: curved band hugging the bottle radius, height capped ~40% of the vessel, tiny band (or none) on thin tubes/cylinders; keep the crisp-TMP-composite pipeline.
- [ ] Fermentation set (vessels, airlocks ×3, balloon), wine bottle + glass, tea/caffeine props, separatory funnel (check pack first), WASTE bin.
- [ ] VFX set — **STATION EFFECTS DONE 2026-07-10 (user):** `StationVfx` (procedural URP particles, ≤60 live per station, radial-dot material built at runtime — zero asset deps): **boiling steam** plume at Heat stations, **freezing frost** sparkle at Crystallise, falling **drip** at Filter, rising **bubbles** at Collect — driven by `ZoneSimStation` occupancy exactly like `SimLoopAudio` (starts when the prop occupies the zone, stops on removal/run end); auto-attached per station by `ExperimentSceneBuilder`. 5 assertions. **One-shot set DONE 2026-07-10:** `EffectVfx` (procedural, shared soft-dot material, auto-destroy) adds **glass-shatter** shard-burst (fired from `BreakableGlassware.Break`, pairs with the glass-break SFX), grade-screen **confetti** (fired from `GradeScreenController.Show` on a pass), and a **flame** set — a looping burner flame layer under the steam at every Heat station (built into `StationVfx`, occupancy-driven) plus a **FlamePop** on burner-ignite AND the methane **splint** flame-test (both fired from `MethaneApparatusRig`). *All station/one-shot VFX now covered; only asset-authored particle art (vs procedural) remains optional polish.* **Spill puddle DONE 2026-07-10 (user):** `SpillPuddle.Spawn` — a wet translucent disc tinted with the spilled chemical's colour drops to the surface below (floor raycast), lingers 3 s, fades 1.2 s, self-destroys; fired by `SpillMistake` (knocked-over bottles) AND `BreakableGlassware.Break` (liquid-carrying vessels). 3 fade-curve assertions. **Ambient atmosphere DONE 2026-07-10 (user), REWORKED for realism (user: "smokes are ugly/pixelated/obscure the view"):** the first pass used a stretched 64px soft-dot with the URP alpha-blend state unset → opaque pixelated blocks. Now uses an **AI-generated soft-smoke texture** (`Resources/smoke-soft.png`, alpha-from-grayscale) on a proper alpha-blended material (`EffectVfx.SmokeMaterial`, explicit SrcAlpha/OneMinusSrcAlpha/ZWrite-off — the missing blend state was the "blocky" bug). `AtmosphereVfx` — a **cool blue-grey vapour puff** that hangs in the aisle just in front of the AC unit and sinks (α 0.68, clustered so it reads; a thin white stream against the white unit was invisible) + a **faint LOW floor mist** (α 0.18; the view-blocking ceiling layer was removed) — plus a **cold-air puff** (`EffectVfx.ColdAir`, α 0.6) spilling out when the lab door opens (`DoorOpener.SetOpen`). All **cool-tinted** so they read against the bright white lab. `Tools ▸ PharmaSynth ▸ Build Atmosphere VFX` (AC vent position is bounds-derived); low-density for Quest overdraw; the Haze velocity-module error was removed (random-direction drift instead). Edit-mode-previewed (build+simulate+DevCapture) across ~8 iterations → soft, see-through, not obscuring. *Opacity/position best judged in-headset (2D capture understates volumetric smoke; reads stronger in stereo) — one-line tweaks in `AtmosphereVfx`.*
- [ ] **Lab lighting brightness** (user 2026-07-10): ceiling fixtures exist but the lab reads dim — brighten lights + emissive fixture panels so it reads as a well-lit laboratory (pre-bake realtime pass now; full lightmap bake stays a later item).
- [ ] **Per-experiment demo video clips + `VideoPlayer` TV screen** (0 VideoClips exist; storyboard expectation). **⛔ BLOCKED — asset-gated:** needs actual demo footage (client-provided or recorded on-device once the lab is content-locked); the `VideoPlayer` TV screen is a quick add once clips exist.
- [ ] UI art skins: grade screen, tablet, HUD pills, station pads (readable primitives today); MainMenu room polish beyond primitives.
- [ ] Proper VR glass shader (stereo-instancing validated, Quest overdraw budget; cheap fallback ready).
- [ ] Lighting: full lightmap re-bake + light probes once layout locks (placeholder realtime today). **⛔ BLOCKED — layout-lock-gated:** a bake is only worth doing after the room arrangement is final (still being tuned); realtime lighting is already brightened for now.

## 4. Audio (infrastructure + CC0 base DONE)

- [x] Gentle lab **ambient loop** — DONE 2026-07-09: AI-generated soft ventilation hum (`Audio/Generated/ambient-lab.wav`), wired at volume 0.15 (Ambient category). **Needs a listen in-editor.**
- [x] Pour / bubble-boil / **burner-ignite** SFX — DONE 2026-07-09: AI-generated (ElevenLabs), wired to their SoundBank keys. **Needs a listen.**
- [x] **Action & apparatus SFX set** (user request 2026-07-09) — CLIPS + ONE-SHOT HOOKUPS DONE: all 13 clips wired (SoundBank 31 entries) AND triggered in gameplay: footsteps (`FootstepPlayer` on the XR rig, 0.75 m stride, teleport-guarded), grab (`GrabPhysicsPolicy.selectEntered`), material-aware drop clatter (`ImpactSound`: glass clink / metal / wood, silent above break speed where shatter takes over), door-swing (`DoorOpener.SetOpen`), ppe-rustle (`PPEController.DonPPE`), socket-snap (`SelectSfx` on station sockets), reaction cues (`LiquidPhysics`: fizz for gas outcomes, mixture-complete chime for colour/ppt/odour, silence for negative tests — `Mishandling.SfxForOutcome`). Ambient hum auto-plays via the existing `AmbientPlayer`. Fix-pass extended: all 51 shelf/bench items now carry DropRespawn + BreakableGlassware (incl. `Reagent_*` bottles) + SpillMistake (pourables) + ImpactSound. Sim LOOPS DONE: `SimLoopAudio` (positional, per-station) driven by `ZoneSimStation` occupancy — boil/drip/hiss/shimmer start when the prop occupies the zone, stop when removed or the run ends. *Only the stir hookup remains, awaiting an actual stirring verb (gameplay feature, not audio).*
- [x] **Upgraded SFX set** (user 2026-07-10, credits authorized) — DONE: AI-generated 7 clips via `elevenlabs-sound-effects-v2` into `Audio/Generated/`, replacing the placeholder CC0 versions and re-pointed in the SoundBank — **`glass-shatter`** (real beaker smash, the headline ask) + `glass-clink` (gentle tap), the four **Pharmee** voice chirps (`pharmee-greet`/`instruct`/`celebrate`/`warn`, volumes raised 0.08→0.3–0.4 so the nicer chirps are audible), and a dedicated **`ac-hum`** HVAC compressor loop now driving the AC unit's `ProximityHum` (was reusing `ambient-lab`). Suite 555 green; **needs a human listen in-editor to confirm the mix.**
- [x] **Audio realism pass** (2026-07-10, smoother-gameplay goal) — DONE: (a) **per-shot pitch variation** on physical one-shot SFX (`AudioService.JitteredPitch` ±8%) so repeated footsteps/clinks/pours/drops don't sound canned, with UI clicks + musical stings (`grade-*`, chimes, `alarm`) explicitly excluded (`AudioService.PitchVaries`); (b) **continuous positional pour sound** — `LiquidPourer` now drives a looping 3D `pour` AudioSource at the spout that swells with flow and fades out when the vessel is righted (`PourVolume` curve; pouring was previously SILENT despite the clip existing); (c) **positional 3D one-shots** — `AudioService.PlayAt3D` (temp 3D source, linear rolloff 0.5–8 m, pitch-varied, self-destroy) now carries drop clatter (`ImpactSound`, at the contact point) and glass-shatter (`BreakableGlassware`) so impacts read spatially instead of flat 2D. 6 assertions.
- [ ] Optional: AudioMixer groups + duck Pharmee voice under SFX. **↷ DEFERRED — no scriptable mixer-authoring API:** creating an `AudioMixer` asset with exposed params + a sidechain send can't be done reliably headlessly; do it by hand in the Audio Mixer window (the `AudioService` already pushes `<Category>Volume` params to a mixer if one is assigned).
- [x] **AC-unit proximity hum** (user 2026-07-10) — DONE same day: `ProximityHum` on `Environment/AC` (found at (3.99, 0.12, −0.83)) — its own looping 3D AudioSource from the `ambient-lab` SoundBank clip, logarithmic rolloff min 0.7 m / max 7 m so it swells as you approach; lazy-starts when the clip exists (survives the clip being supplied later). Grab/footstep/drop SFX re-verified as wired (§4 hookups + stride-math assertions).
- [x] **Footstep audibility + hover SFX** (user 2026-07-10 batch 4) — DONE: the generated `footstep.wav` was effectively inaudible (peak **4.3 %**, RMS 452 — masked by AC/music), so it was peak-normalised in place to ~85 % (also fixed `glass-clink` 3.2 %→72 % and lifted `grab`→62 %); the trigger (`FootstepPlayer`, head-tracked, 0.75 m stride) was already correct. Added a soft synthesised `hover` blip that plays on hover-enter (globally throttled 0.09 s) and centralised the `grab` cue into `HoverHighlight.OnSelect` so it fires **once** on every grabbable (removed the duplicate in `GrabPhysicsPolicy`). *Loudness balance wants a headset listen — single SoundBank-volume tweaks.*

## 5. UI & flow

- [x] **Cube spawn room + spawn/teleport/reset flow** (user 2026-07-10) — DONE: the `MainMenu` scene is now a **solid, fully-enclosed futuristic cube** (`Tools ▸ PharmaSynth ▸ Build Menu Cube Room`, re-runnable — dark walls + cyan emissive trim/panels + a glowing floor launch-pad + soft fill lights; old open `MenuRoom` dressing hidden; no skybox leak). Panel trimmed to exactly **Laboratory / Settings / Quit** (Tutorial button removed — reached in-lab via Pharmee's episode picker; the vestigial `ExperimentSelect` roster + `Results` button hidden, feature preserved for re-enable). No HUD here (HUD lives only in `SampleScene`). **Laboratory** fades→loads the lab where the HUD appears and **Pharmee greets** (`PharmeeGatekeeper.SpeakWelcome` on `Start`; `narration`+`ppe` refs were unwired and are now hooked — also gives Pharmee his spoken gate lines). **Quit** (HUD) already returns here. **Reset** (HUD) rewired to `PharmeeGatekeeper.ResetToEntrance`: props/bottles re-seated + **wearables removed** (`PPEController.RemovePPE`) + teleport to `FrontDoorSpawn` + gate re-`Blocked` + Pharmee greets, all inside one black fade. Two music beds: cube room `MenuMusicPlayer` (`music-menu`) + new `LabMusicPlayer` (`music-lab`, SoundBank entry added, **clips supplied by user later** — drop-in, no code). Self-tests 511/511; cube DevCapture-verified. *Play-test the four transitions in-headset; cube emissive tuned for VR comfort but tweakable.*
- [x] **Cube room polish** (user 2026-07-10 pt 2) — DONE: (a) **Movement** — the menu rig had `LocomotionMediator`+`XRBodyTransformer` but no move/turn providers, so you couldn't walk; copied the lab rig's `Move` (`DynamicMoveProvider`) + `Turn` (`SnapTurnProvider`+`ContinuousTurnProvider`) onto `MainMenu`'s `Locomotion` (via `ComponentUtility` across an additive scene load), re-pointing mediator/camera/controller refs to the menu rig (input-action asset refs preserved). (b) **Neon** — `MenuCubeRoomBuilder` extended: brighter cyan emissive, holographic **wall frames** (border+centre line inset proud of each wall) + a **floor grid** + glowing launch-pad + brighter cyan fill lights. (c) **Panel restyle** — `MenuCanvas` recoloured to the lab dialogue-box theme (panel bg `(0.05,0.07,0.11,0.97)`, translucent-navy buttons with cyan outline + cyan text, Quit warm-accented). (d) **Spawn VFX** — `SpawnBurstFX` scene-singleton + `SpawnVfxBuilder` (`Tools ▸ PharmaSynth ▸ Build Spawn VFX`): a one-shot cyan particle column that rises from the player's feet on every spawn/teleport/reset (playOnStart in both scenes + fired from `PharmeeGatekeeper.TeleportToFrontDoor`). DevCapture-verified. *Gotcha logged: after editing an editor builder, force a recompile before re-running its menu or it runs stale.*
- [x] **Menu scene XR-ization** — DONE 2026-07-09: full XRI rig activated (it was the complete starter rig — poke/near-far/teleport interactors + locomotion), flat camera disabled, all three canvases pointed at the XR camera, `ExperimentSelect` converted overlay → world space (angled side panel right of the menu, `TrackedDeviceGraphicRaycaster` added), `XRUIInputModule` replaces `InputSystemUIInputModule` **with mouse input kept on** so desktop testing still works. DevCapture-verified from the rig's viewpoint. *Needs one desktop Play sanity pass + the on-device check later.*
- [x] Settings **apply-listeners** — DONE 2026-07-09: `ComfortApplier` on `Services` (SampleScene) subscribes to `SettingsService.Changed` and applies: HUD/text scale (scales `HudRig` root — frustum-fit divides by scale so everything renders bigger, baseline cached to avoid compounding), snap-turn angle → `SnapTurnProvider.turnAmount`, vignette intensity → `TunnelingVignetteController` aperture (1→0.35 curve), subtitle pacing → `SetSubtitlePace` seams on `PharmeeBrain` + `PharmeeGatekeeper` (dwell = base/speed). Pure curves in `ComfortMath`, 10 assertions. *(Menu scene has no comfort targets; prefs persist and apply on lab load.)*
- [x] Results/History **screen UI** — DONE 2026-07-09: MainMenu gained a **Results** button (cloned Settings styling, slotted above Quit) opening `ResultsPanel`: per-experiment PASS/TRY/-- badges + best grade/mastery/attempts + overall count (`ResultsHistoryController`, verified rendering all 11 rows), **Export CSV** → `persistentDataPath/pharmasynth_results.csv` with the path echoed on screen, Close. *(In-headset "share" beyond the on-disk CSV = descoped analytics; revisit only if the client asks.)*
- [x] **PPE gating** (user 2026-07-10) — DONE same day: see the PPE completion entry in §3 (gate requires coat+goggles+gloves; per-piece visuals).
- [x] **Quiz tablet placement audit** (user 2026-07-10) — VERIFIED same day: `PostLabTablet` sits front-centre of the lab at (0, 1.5, −0.55) beside the `LabTablet` Procedure board and the GradeScreen (a coherent review corner visible from the main aisle); its Panel stays hidden until the data-sheet phase, then `FaceCamera` turns it to the player. Render-verified readable: title, question counter, 4 option buttons, yield ±5 stepper, Submit — all wired (runner + QuizBankLibrary).
- [x] **Watch-guide accuracy audit** (user 2026-07-10) — VERIFIED same day: the wrist summary (current step/progress/mastery) and the holo checklist both render LIVE from `runner.Graph` — the same TaskGraph the grader scores (☑ complete / ▶ available / ☐ pending via `TaskGraph.IsComplete/IsAvailable`, phase-grouped, refreshed every frame while shown) — accurate by construction, no duplicated state to drift. **Forced-restart paths:** reagents exhausted → `ReagentSupplyMonitor.SupplyExhausted` → gate `SupplyPrompt` → Pharmee offers "Restart period" (attempt recorded failed, stage reloads) or "Keep trying"; broken tools NEVER force a restart (client policy 2026-07-09: Sanitation penalty + shelf replacement via `DropRespawn.GoHome`) — and reagent lost in a break/spill drains into the starvation path, so every dead-end still reaches the restart prompt.
- [ ] HUD comfort validation on-device: ScreenLocked (current, storyboard) vs LazyFollow (built-in fallback mode) — decide after headset testing.
- [x] Pharmee **random idle chatter / comments on player actions** — DONE 2026-07-10: `PharmeeLines` (pure, tested line pools) drives VARIETY so Pharmee no longer repeats one canned line — rotating greetings/celebrate/encourage/warnings, **occasional praise folded into the next step instruction** on each task completion, and **ambient idle chatter** (`PharmeeBrain.Update`: after `idleChatterGap`≈16 s of quiet mid-run he offers a friendly/safety tip). 3 line-pool assertions.
- [x] **Guided Lab Tour — location-triggered** (storyboard 2026-07-10) — DONE: the Lab Tour was a single "roam freely" line; now `LabTourGuide` on Pharmee narrates each area **as the player walks up to it** (intro → `LabTablet`/`EquipmentShelf`/`ReagentShelf` proximity beats, each folding in a UI tip → sign-off), pure `FirstUnvisitedInRange` core tested, timed `PharmeeLines.TourBeats` fallback, poke-to-end preserved. See [storyboard-reconciliation.md](storyboard-reconciliation.md).
- [ ] Cutscene staging polish: prop staging per beat, wine "7 days & 7 nights" time-skip montage + tasting finale (data + copy exist). *(Storyboard marks these as generic `[CUTSCENE]` placeholders — the game's 44 authored cutscene SOs already exceed it; per-beat staging is the open polish.)*
- [x] **Typewriter dialogue + talking blips** (user 2026-07-10) — DONE: NPC lines now **type out char-by-char** (TMP `maxVisibleCharacters`, ~32 cps) instead of flashing all at once, on BOTH the world-space bubble (`NPCNarrationController`) and the screen HUD bar (`HudDialogueBar`, mirrored via `LineStarted` + shared `TypeCps()` so they stay in sync). A per-speaker **talking blip** fires every ~2 revealed characters (pitch-varied) — two AI clips generated + wired: `pharmee-blip` (RobotNPC) and `jimenez-blip` (DrJimenez bubble). Instant `BeginLine` path preserved for cutscenes/tests. **Skip-to-complete (2026-07-10):** the skip button's first tap fills the current line instantly (`NPCNarrationController.SkipNarration` → `IsRevealing`/`_skipReveal`, HUD mirrors the shared `VisibleCount` so both fill together), a second tap advances — fast readers aren't forced to wait, one tap never skips unseen text. **Dialogue ducking (2026-07-10):** `DialogueDucker` (auto-attached by `AudioService`) ref-counts speakers and dips the ambient + music beds to ~45% while any NPC talks, easing back after (pure `DuckTarget` tested). *Blip volume / type-speed / duck depth feel want an in-headset listen (single-value tweaks).*
- [x] **Hover-info panes (learn-as-you-play)** (user 2026-07-10 batch 4) — DONE: point the right-hand ray (gaze fallback) at any reagent bottle, apparatus or NPC and a smoothly fading/scaling card names it and explains it — what-it's-for + quick how-to for equipment, trivia + hazard for reagents, role for Pharmee/Dr. Jimenez. `HoverInfoPanel` (world-space card, eases in/out, rides a readable ≤1.5 m in front along the sightline, billboarded) + `HoverInspector` (per-frame raycast resolver) + `LabInfoDatabase` (pure: 31 equipment + 37 reagents + 2 NPCs, keyed by `LiquidPhysics.currentChemical` / `LabItem` / prop name / NPC marker). Built by **Tools ▸ PharmaSynth ▸ Build Hover Info Panel**; card layout DevCapture-verified; 25 new assertions. *Distance/opacity/dwell feel wants a headset pass.*
- [x] **Table-pad + wrist-holo text fixed** (user 2026-07-10 batch 4) — DONE: LiberationSans SDF (the tablet/holo font) is missing `☑ ☐ ▶ → Δ ↑` (they rendered as blank boxes; the `- Fallback.asset` the user was editing has 0 glyphs). `GlyphSafe.Sanitize` (pure, tested) maps them to font-safe forms at display time (→ `->`, ↑ `(g)`, Δ `(heat)`, ⇌ `<=>`, ▶ `»`), and the checklist markers became coloured font-safe `• » □`; applied in `TabletChecklistController` (procedure pad + reaction footer) and `WristWatchController` (mini-panel + holo board). Source data left clean. *Marker/reaction rendering confirmed by glyph analysis; see it live in a Play-mode experiment.*
- [x] **Watch gesture panel verified** (user 2026-07-10 batch 4) — VERIFIED + fixed: the wrist-flip (supination + gaze, only during an experiment) shows the compact MiniPanel AND the large holo checklist — all refs (runner/anchor/head/holo) were wired; the one gap was a **null `summaryText`** (MiniPanel rendered blank), now pointed at its `Summary` TMP so it shows step/progress/mastery. Holo board also benefits from the glyph fix above. *Watch MODEL is still a placeholder cube; `toggleAction` fallback left unset (gesture-driven) — add a dev key if headset testing needs it.*
- [ ] Dialogue copy pass for all 11 experiments → **client sign-off**.

## 6. On-device & release (needs the Quest 3)

**⛔ ENTIRE SECTION BLOCKED — no headset on hand:** every item here requires the physical Quest 3 (perf/comfort/hand-tracking/FFR/keystore/UAT on real hardware). Cannot be actioned in-editor. **Escalate to the client per the W5 deadline (Aug 4–10) if the device still hasn't arrived** (contractual risk — see first item).


- [ ] **Headset escalation**: if no Quest 3 by W5 (Aug 4–10), flag the client — contractual risk.
- [ ] Day-1 on-device: 90 Hz hold worst-case (72 fallback), comfort pass, wrist-gesture ergonomics, hand-tracking pass, MSAA 4× vs fill-rate, FFR validation, HUD mode decision.
- [ ] Perf hardening: ASTC/atlasing, draw-call audit vs ≤150, tris ≤1.2M, rigidbody budget ≤40 kinematic-on-grab. **↷ Device-side items still need the Quest**, but a **proactive in-editor pass landed 2026-07-10** (goal: smoother UX): eliminated the two per-frame GC allocations in the hot loops — `PharmeeMover` reuses a cached anchor list (was `new List<Vector3>` every frame for the whole run) and `ProximityLabel` caches the item's renderer array (was `GetComponentsInChildren` every frame while a tag showed) — and **gated the mirror's per-frame reflection render on the player actually looking at it** (`MirrorPlane.ShouldRender`: near + in-front + facing-toward, ~105° generous), skipping the scene's single most expensive per-frame op whenever you're turned away. 8 assertions; HUD timer (1 s throttle) and supply monitor (2 s poll) audited already-clean.
- [ ] Android keystore + signing docs.
- [ ] Full revision-checklist UAT + ISO/IEC 25010 acceptance instrument.
- [ ] Final APK + user guide + technical handover + implementation summary.

## 7. Client decisions (see client-signoff-request.md)

**⛔ SECTION BLOCKED — awaiting the client:** these are sign-offs/decisions only the client can make (chemistry routes, scoring weights, Jimenez model, dialogue copy). The implementation already follows the resolved defaults; `client-signoff-request.md` is the outbound ask. Nothing actionable in-editor until they respond.


- [ ] Chemistry sign-offs — **MANUSCRIPT-VERIFIED 2026-07-10** (see [manuscript-reconciliation.md](manuscript-reconciliation.md) §3, evidence with line citations): (a) **Benzoic Acid** — the printed Appendix C Exp 4 procedure is a *copy-paste of the Ethyl Alcohol fermentation* (confirmed defect); materials list correctly names Benzaldehyde → game's benzaldehyde+KMnO₄ route is right, treat Exp 4 procedure as errata; (b) **Acetanilide** — Appendix C specifies **acetyl chloride** (game matches; only the intro *prose* said anhydride); (c) **Benzamide** — the "Sodium nitrate" in the reagents header is a typo; the procedure body says **sodium nitrite** (game matches). *Remaining: client rubber-stamp of these three errata-corrections, + confirm the game-authored Methane/Aspirin/Caffeine chemistry (no manuscript source).*
- [ ] **Scoring-weight sign-off** (hard W2 exit criterion — still open).
- [x] Yield-in-grade decision — RESOLVED: record-only (§1). Breakage policy — RESOLVED: lose points + shelf replacement everywhere incl. exams, never a forced restart (matches implementation). Wine rubric — RESOLVED: standard 6 categories.
- [ ] Dr. Jimenez budget/model — **ON HOLD: the client may supply their own model of him**; keep the primitive stand-in until then. Analytics-descope confirmation still open.

## 8. Process

- [ ] User commits checkpoints regularly (two editor crashes have occurred); Claude commits only when asked; retire the dead `feature/asset-intake` branch.

## 9. W5.7 "things to do" batch (2026-07-11, suite 613→751) — landed + follow-ups

## §12 W5.10 workspace cleanup (2026-07-12)

**DONE (suite 963→969):** floating stale Methane labels purged from the workspace (`Purge Stale Workspace Labels`); center-table gantry turned into a real equipment shelf (`Build Workspace Shelf`, 3 tiles + lip + colliders); Tripo reference images (~17 MB) purged out of Assets to the gitignored raw-art-sources; 3 misnamed Tripo prefabs renamed so the litmus/cotton single models now appear instead of procedural.

- [ ] Human play-mode glance (W5.10): confirm nothing floats over the workspace, and equipment placed on the new gantry shelf rests stably. The shelf uses the bench material (light tiles) — say if a different look is wanted.
- [ ] Optional: author a `FilterPaperSingle` Tripo model (currently the only consumable single still procedural).

## §11 W5.9 flow + manuscript sweep (2026-07-12)

**DONE (suite 920→963, see CLAUDE.md W5.9 block):** campaign-complete celebration (was a dead-end promising a nonexistent "next challenge") · floored grade display (no more "90% / TRY AGAIN") · Abort() ends attempts on HUD Restart (no zombie runs; grade/quiz UI cleaned up) · fail-path "Choose Another" button (was Retry-only) · supply-prompt un-latch + no failure-cutscene + faded teleport · ambient NPC chatter suppressed during the scripted review · fader callback composition + Loading watchdog · quiz clock frozen on every path · empty-quiz-bank parity · door never traps an inside player · manuscript fixes (benzoic propyl ester, chloroform dichromate test, Mixed Fruit Juice, KI + diluted acids with rule inputs in lockstep, compounding quiz Q3).

- [ ] **Human Play-mode pass (W5.9):** fail an experiment → "Choose Another" returns to the door picker; retry → clean re-arm; starve a bottle → Keep trying → starve again → re-prompt appears; HUD Restart during the quiz → no floating tablet/grade card; supply restart shows NO failure cutscene and fades; Jimenez's briefing/verdict never interrupted; pass the final experiment (demo unlock-all is fastest) → campaign-complete line + confetti; benzoic ester test with propyl alcohol reacts; chloroform dichromate test present and reacts.
- [ ] CLIENT: Chemical Compounding scope decision (manuscript-reconciliation §7) + Safety-mastery coupling + per-attempt history (client-signoff-request W5.9 section).

## §10 W5.8 follow-ups (experiment-functions overhaul, 2026-07-12)

- [ ] **Human Play-mode feel pass (W5.8)**: pour into the main beaker (visible fill + adopted chemical + reaction popups), stir the Benzamide beaker with the rod (held AND on table), grind the Methane mortar, weigh salicylic on the Aspirin balance, strike a match on the striker/box → light the burner → watch the temp label climb, methane splint pop with a lit match, settle-freeze feel (items at rest stop jittering), rack tubes + spares reachable, popup/label text sizes in-headset.
- [ ] Optional: a distinct SHAKE verb (descoped in W5.8 — stir covers the manuscript intent; revisit only if the client asks).
- [ ] Optional: revert knob — if the client dislikes burner-gated heating, clear the `ZoneSimStation.SetIgnitionGate` wiring in `WireVerbControllers` (one block).
- [ ] Optional: extend `WeighStation` to more modules (Ethyl Alcohol "weigh 4 g yeast", Acetone distillate weigh) — the infra is generic; add `sim: 7` stations + bindings via `Apply W5.8 Verb Data`-style edits.

**DONE (see CLAUDE.md W5.7 block for detail):** velocity-tracked grabs (no wall phasing) · visible pour arcs + wired shelf/cabinet bottles · single holo procedures board (LabTablet + wrist MiniPanel retired, focused checklist = overlap fix) · Demo Mode (config-gated button, separate save, unlock-all, HUD auto-complete, infinite supply, ready-made DemoKit) · ILO intro beats (verbatim Appendix C) · post-experiment review flow (Jimenez quiz briefing → tablet quiz → spoken verdict → full-reset return → entrance debrief; retry re-arms at door; quiz never score-gated) · hazardous-mix effects + alarm + overheat-ruins-batch + graded penalties · ONE wide landscape center table (layouts remapped) · 56-material raw-reagent catalog + 3 stocked cabinets + litmus/matchstick interactions · NPC voice-over pipeline (manifest 189 lines ≈ 6.3k ElevenLabs credits).

- [ ] **Voice generation run** — needs the user's `ELEVENLABS_API_KEY`: `.\Tools\voice\generate-voice.ps1 -SampleOnly` → human voice approval (sign-off §6) → full run → `Voice ▸ Import & Wire Voice Clips`. Cost ≈ 6.3k credits (fits a $5 Starter month).
- [ ] **W5.7 Play-mode feel pass** (human): pour arc/droplet density, velocity-tracked grab feel, demo run-through end-to-end, review-corner staging/timing, alarm volume + vignette intensity, holo board readability, cabinet reachability.
- [ ] **Ceiling light panels** — batchmode captures showed magenta panels (likely async-shader placeholder, not real breakage): confirm in-editor; if genuinely magenta re-run `Brighten Lab Lighting`.
- [x] Two clamped overlaps — GONE 2026-07-12 (W5.8): `Tidy Experiment Layouts` re-zoned all 10 layouts onto the `LayoutTidyMath` grid; `LayoutSpacingSuite` pins deck-bounds + pairwise ≥0.14 m + 0.5 m station pitch.
- [x] **Cabinet/consumable art pass (2026-07-11, credits spent):** AI-generated 3 PBR materials via `realistic-textures-3-0` in `Art/Generated/Materials/` — **LabLaminate** (cabinet carcasses), **Cardboard** (consumable boxes), **BrushedSteel** (ice bucket) — wired into `ReagentCabinetBuilder` (load-or-fallback) and rebuilt; the gray-box placeholders now read as white laminate casework + kraft cardboard boxes + a steel ice pail. *Note: MCP 3D-mesh generation (Tripo P1) is confirmed BROKEN here — the reference-image instance ID loses precision through MCP JSON (the CLAUDE.md gotcha), so meshes are generated by the USER in the Unity AI panel from reference images.*
- [ ] **Tripo the remaining consumable models (user, AI panel):** reference images ready in `Art/Generated/Refs/` — `MatchboxRef`, `FilterPaperRef`, `CottonSwabsRef`, `LitmusBoxRef`, `PowderJarRef`, `SeparatoryFunnelRef`, `FlorenceFlaskRef` (ice bucket DONE + auto-swapped into the cabinet). Save each generated prefab into `Art/Generated/Refs/` named **Matchbox / FilterPaper / CottonSwabs / LitmusBox / PowderJar / SeparatoryFunnel / FlorenceFlask** — `Build Reagent Cabinets` auto-swaps the box placeholders for the first four on rebuild; sep-funnel/Florence/jar wiring is a small follow-up once they exist.
- [x] **Cabinet layout v2 (user 2026-07-11):** 2 units (third removed), lifted 0.3 m off the floor, 7 slots/shelf, printer relocated to the free side table + grabbable, liquid fills colour-baked (visible through glass) + colour-coded caps on opaque vials (near-white chems get neutral grey), Tripo ice bucket in place.
- [ ] Cabinet bottles still use `ProximityLabel` tags — extend `Generate Reagent Labels` (LabelForge) over `ReagentCabinets` for composited labels (pairs with the open "label aesthetics redo").
- [x] Consumable STAGING (matches) — DONE 2026-07-12 (W5.8): Heat experiments stage 2 matchsticks + a striker block on the table (`StageConsumables`; strike-to-light `Matchstick.ShouldStrike`, burner bases are strikers too). Litmus stays cabinet-side (not per-step-critical); ice `ColdSource` behavior stays optional polish.
- [ ] Demo HUD buttons + Demo menu button want a quick style pass in-headset (amber theme chosen to read as "special mode").

---

**Reading the board:** engine, data, and the full client-confirmed game loop are done and regression-locked (385 assertions). What remains is a production pass — per-experiment observation wiring (§1), the physics/interaction pass (§2), art/audio (§3–4), small UI completions (§5) — then the externally-gated tail: client sign-offs (§7) and the Quest-3 on-device week (§6). Suggested order: §2 physics pass → §1 test vessels (Tier-1 first) → §5 menu XR + settings listeners → §3/§4 art-audio → device QA.

## §13 Queued playtest issues & next-session prompts (user, 2026-07-12)

The user's in-headset playtest findings + planned work, queued verbatim-in-spirit with root-cause hints from the W5.8-W5.11 sessions. Asset creation is greenlit (user has credits — flag AI-generation spends first).

### 13a — Apparatus assembly kits on the workspace shelf
- [ ] Group apparatus that belong together, learned from the **manuscript Equipment & Apparatus lists** (now extracted per experiment in `experiments-reference.md`): the HEATING SET (Bunsen burner + tripod + wire gauze + iron ring + retort/iron stand + clamps + crucible tongs; decide whether the alcohol burner gets its own set), flask+funnel sets, etc.
- [ ] **Test-tube rack kits ×4**: one with regular test tubes, one with HARD-GLASS tubes (recolor/tint to distinguish — 4 sets of hard-glass tubes wanted), one with the other rack-holdable tube kind, one empty spare rack. Test-tube brush staged beside them.
- [ ] Place all kits on the workspace-shelf gantry rows (`Build Workspace Shelf` exists; user already hand-placed some apparatus — **their DropRespawn homes are NOT adopted yet → run `Re-Home Scene Items (Adopt Current)` or the kits builder should SetHome**). Generous duplicates of high-use glass (test tubes, beakers, vials) but not overflowing; tight spacing without overlap (reuse the `LayoutTidyMath`/`WorkspaceShelfMath` spacing discipline).

### 13b — Snap/stick assembly mechanics + interaction animations
- [ ] Apparatus that belong together should SNAP/stick when brought close (burner⇄tripod⇄gauze⇄flask; watch glass onto beaker mouth; etc.).
- [ ] **Detach = the ACTIVATE click, not Grab** (grab is forceful and would dismantle assemblies messily). Grabbing a stuck group = move the WHOLE group preserving formation; click-to-detach first to separate a piece. (XRI: consider socket-style attach points + an `activated` handler that releases the joint before allowing individual grab.)
- [ ] Interaction-upon-contact animations with SUPPORT TEXTS: e.g. test-tube brush scrubbing a dirty tube — each swipe reduces a visible dirtiness % until it relabels "Clean Test Tube". Same support-text pattern for burner temperature, flask contents %, processes, scale weight (much of this exists: StationStatusLabel / VesselStatus / FloatingText — extend, don't duplicate).
- [ ] Sweep the manuscript for interactions we haven't modeled yet and add them.

### 13c — Scooping + weighing flow
- [ ] Solid reagents that are scooped (scoopula/spatula): each scoop transfers a FIXED amount into the target container (beaker / watch glass); the container's contents visual + live grams text increase per scoop (WeighStation's scale text already live — wire scoop deltas into it). Double-check which reagents are by nature solids-to-scoop vs liquids-to-pour (ChemicalData.state audit).

### 13d — Breakage & respawn round 2 (user-reported)
- [ ] **Items carried to the table vanish back to their shelves mid-run** (both tour + campaign) → experiments impossible. ROOT CAUSE: `DropRespawnMath.ShouldReturnHome` (25 s idle + >0.4 m from home) reclaims player-staged items. Fix direction: adopt-new-home-on-player-release, or suppress return-home while a run/tour is active, or only return items below the deck.
- [ ] **Break-respawn loses the bottle**: after a non-breakable reagent (e.g. Diluted Acetic Acid) spills out and/or a breakable respawns, only the lid + name tag come back — body/contents gone, bottle unusable. Investigate `BreakableGlassware.Break`→`GoHome` + `EnsureLiquidVisual` renderer state + supply restore (respawn must restore FULL bottle + max contents). Lab-tour mode: unlimited regenerates.
- [ ] **Material-nature audit**: some non-glass items (scale/Balance, Bunsen burner…) still play glass-shatter on collision and can "break" — audit `Mishandling` lists + any scene objects wired by the old `PhysicsAudit` Reagent_* broadening; assign nature-correct sounds and breakability.
- [ ] **More leniency**: only extreme falls / very fast collisions should break or trigger the shatter sound; current soft contacts still trigger sounds. (Threshold is 4.5 m/s + settle-freeze since W5.8 — user still finds it touchy in-headset; consider raising further + gating the clink volume by impact speed.)

### 13e — Pour still failing in-headset (user report AFTER the W5.8 fix)
- [ ] User still cannot pour reagents into vials/tubes/beakers in the headset — "the beaker won't catch the spills". The W5.8 raycast/wake fix is suite-green in edit mode, so this needs LIVE play debugging (spout position while tilted? stream vs receiver mouth offset? XR hand tilt angles? scene-authored bottles missing wiring?).
- [ ] Add **dynamic liquid level** display so the amount is trackable (fill shader exists; verify visibility at vial scale) + support texts; colour changes on mixtures (exists — verify in-headset); re-audit which reagents are liquids vs solids (ties to 13c).

### 13f — UI & dialogue polish (user-reported)
- [ ] **Wrist watch panel**: instruction step renders as ONE continuous row — wrap the text and make the panel scrollable.
- [ ] **Pharmee dialogues incomplete** — some lines cut off; audit each (narration dwell vs line length; `lineSeconds` vs GlyphSafe/TMP overflow).
- [ ] **Pharmee dialogue fires when activating the watch panel** by hand gesture — suppress chatter on gesture/panel activation.
- [ ] **Shatter message shows the CODE name** (e.g. prefab/GO name) — always use the display name (BreakableGlassware label binding: builders pass displayName, but scene-wired/late-bound items fall back to `name`; audit `Awake` fallback + PhysicsAudit wiring).
