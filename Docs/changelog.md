# Changelog — session history (W1 → W5.10)

Dated batch notes moved out of CLAUDE.md on 2026-07-12 (W5.11 docs consolidation). Preserved verbatim because git commit messages are thin (the user commits checkpoints). Newest first within each section. For CURRENT system behavior use `gameplay-flow.md`, `systems-reference.md`, `experiments-reference.md` — this file is history only.

---

## CURRENT STATE (2026-07-12, end of W5.10 — suite 969)
Engine, all 11 experiments (data + auto-built stages), and the **full client-confirmed game loop** are done and regression-locked. A human play-tests by pressing Play (MCP play mode is unreliable).

**2026-07-11 W5.7 mega-batch ("things to do", plan `things-to-do-fuzzy-kahn.md`, suite 613→751):** (a) **Grab collision** — all 42 prefab + 58 scene `XRGrabInteractable`s velocity-tracked (`GrabTuning`, menu `Wire Grab Collision`); held items now stop against walls/furniture. (b) **Visible pours** — all shelf/cabinet bottles wired with `LiquidPourer`+`SpillMistake`+`HazardousMixReactor` (`ShelfPourWiring`, menu `Wire Shelf Pourers`); pour ARC LineRenderer self-creates; droplets bigger; `Resources/FxParticleUnlit.mat` guards device-build shader stripping. (c) **One procedures display** — entrance LabTablet + wrist MiniPanel retired; the holo board is THE panel (header + `ChecklistPager.BuildFocusedText` — active phase detailed, others collapsed = the text-overlap fix — + reaction footer); menu `Consolidate Procedure Panels`; tour re-seeded. (d) **Demo Mode** — `StreamingAssets/demo-config.json` (persistentDataPath override wins; menu `Demo ▸ Demo Enabled (persistent override)`) reveals an amber **Demo Mode** button in the cube room (`Demo ▸ Build Demo Menu Button`); a demo session = separate `pharmasynth_progress_demo.json` save, ALL periods unlocked (`ProgressionFlow.Create` folds `DemoSession.Active`), HUD Skip Step/Finish Exp./Auto Quiz cluster (`Demo ▸ Build Demo HUD`, `DemoActions`), infinite reagent refill. (e) **ILO opening dialogue** — verbatim Appendix C objectives injected as intro-cutscene beats (`IloCopy`, menu `Inject ILO Beats`; Methane/Aspirin/Caffeine game-authored → sign-off §5). (f) **Post-experiment review flow** (user-approved): Running→TestsDone→**QuizIntro** (clock FROZEN `runner.FreezeClock`, Pharmee congrats, fade-teleport to `ReviewCornerSpawn` facing PostLabTablet+Jimenez)→Jimenez 2-beat brief (`ExaminerNPC.SpeakLine`)→**QuizTime** (`postLab.Open()`, autoOpen=false in scene, never score-gated per manuscript)→**ScoreReview** (grade+outro, then Jimenez pass/fail verdict + Pharmee remark; banded pools — numbers stay on the card)→Continue→**Returning** (one fade: stage re-seat + `WearableReseat` + teleport home)→**Debrief at the entrance** (quiz congrats + banded remark)→UnlockAnnounce→Blocked. Retry from review = clean re-armed attempt at the door; dev F-key cascades safely; menu `Build Review Corner`. (g) **Error effects** — `HazardousMix` (acid+hypochlorite→ToxicGas, oxidizer+Flammable→FireBurst, liquid-into-conc-acid→Spatter, else fizz) on the `WrongReagentMixed` seam via `HazardousMixReactor` (2 s/vessel cooldown, graded per §3.7 matrix); `LabAlarm` (red flashing fixture + alarm SFX, menu `Build Lab Alarm`); `ScreenFader.PulseWarning` vignette; `OverheatEffects` at Heat stations (smoke + vessel becomes `Chem_RuinedMixture` + Overheat mistake); new `EffectVfx` kinds Smoke/FireBurst/Spatter/ColorFlash; `ChemicalData.isOxidizer/isConcentratedAcid` stamped by `Audit Chemical Hazard Flags`. (h) **ONE wide landscape center table** — `Merge Center Tables` rigid-remapped island `Table_1` (rot 90°) to (0, 0.91, −3.3), retired `Table_1 (1)`, rewrote all 138 layout positions + methane stage (98 exact + 40 clamped onto the deck — two clamped pairs overlap in Acetone/Benzamide, cosmetic), sinks were island children (rode/retired automatically). (i) **Raw reagents** — `RawReagentCatalog` (56 manuscript materials incl. litmus/matches/cotton/filter/ice consumables), `Generate Raw Reagent Data` (ChemicalData SOs + library), `Build Reagent Cabinets` (3 wall units at x≈4.3, 43 stocked, 14 already on shelf; pourable+grabbable+hover blurbs via `LabInfoDatabase`→catalog fallback); interactive `LitmusStrip` (pH tint) + `Matchstick` (ignites near hot stations); demo sessions also spawn the module's ready-made product (`DemoMode.ProductFor` → DemoKit vial). (j) **NPC voice-over pipeline** — `VoiceBank` (speaker+text-hash → clip), `NPCNarrationController.BindVoice/ResolveVoice` (voiced lines suppress blips, dwell ≥ clip length; missing clip = blips as before); corpus = code lines (`VoiceCorpus`) + cutscene beats; menus `Voice ▸ Export Voice Manifest` (**189 lines, 12.7k chars ≈ 6.3k ElevenLabs credits — fits a $5 Starter plan**) and `Voice ▸ Import & Wire Voice Clips`; generation = `Tools/voice/generate-voice.ps1` (needs user's `ELEVENLABS_API_KEY`; `-SampleOnly` first for the voice listen). *Pending human: Play-mode feel pass (pour arc, velocity-tracked grabs, demo run-through, review-corner staging, alarm volume), ceiling-panel material check (batch captures showed magenta — likely async-shader placeholder), voice sample approval + full generation.*

**2026-07-11 follow-ups (suite 751→760):** hover cards suppressed on HELD items (`HoverInspector.IsHeld`); demo button lazy-resolve fix (`DemoMode.EnsureResolved`); `Tools ▸ PharmaSynth ▸ Select Movable Furniture` (re-enables picking + colliders on tables/sinks/cabinets/shelf roots/speaker/printer/AC — re-run after Unity restarts); cabinet v2 = 2 raised units at x≈4.32 (user-moved transforms preserved across rebuilds), printer relocated, 34 glass bottles breakable in place; lockers relocated (backs to east wall x=4.55 facing west; `DoorOpen` set += CoatPrompt/ReadyPrompt since the locker is now inside the lab); Tripo auto-swap convention = save prefabs to `Art/Generated/Refs/<Name>.prefab`; **ReagentShelf moved to the west wall** (bottles in `Environment/3x4` cubby unit at x≈−5.0, re-homed); **LabelForge extended to the cabinets** (curved band outward = the owning `CabinetUnit_*`'s open face = −unit.right; non-tubular `Raw_*` boxes/bucket get a FLAT `NameLabel` quad on the open face; tiny singles skipped — 59 labels total); **end products hidden in regular mode** — `DemoMode.IsEndProduct` + `EndProductVisibility` on ReagentShelf/ReagentCabinets roots (Play-mode SetActive(false) unless `DemoSession.Active`; menu `Wire End-Product Gate`; 3 bottles gated). DevCapture gotcha: NEGATIVE yaw misparses (bottles seemed to float in the sky) — use 0–360 (e.g. 270, not −90).

**2026-07-11 polish round 2 (suite 760→761):** (a) **Labels no longer half-buried** — `LabelForge` band radius was `r*0.92` (INSIDE the transparent glass → text read as embedded); now `r*1.0+0.006` so the 140° arc floats just proud of the widest extent (flat box plates likewise +0.007). Re-run `Generate Reagent Labels` (59 labels, DevCapture-verified crisp on the glass front). (b) **Breakage far less twitchy** — root cause: velocity-tracked grabs (Batch A) keep the held Rigidbody DYNAMIC, so `BreakableGlassware`'s old `isKinematic` "in a hand" guard failed and any wall/bottle brush shattered it. Now guards on `XRGrab.isSelected` (held → never breaks, any speed) + a 0.35 s post-release grace window (ignores the let-go velocity spike); `Mishandling.DefaultBreakSpeed` raised 2.8→4.0 m/s (≈0.8 m drop). (c) **Wrist procedures board facing CONFIRMED** — `HoloChecklist` has `FaceCamera` (yAxisOnly, faceTowardCamera=false) + valid `Camera.main`; a parked-and-rendered capture proved the text reads right-way-round facing the player (this was the Batch B FaceCamera addition; already correct). (d) Printer (`Environment/Printe` @ 4.02,1.01,−5.20) confirmed selectable via `Select Movable Furniture` (has collider, no label — appliances get none).

**2026-07-11 consumable dispensers (suite 761→769):** the four paper/stick consumables now work as **fixed single-piece dispensers** (user: "put the box on the shelf; grabbing it pulls out a single piece"). `ConsumableDispenser` (Interaction) keeps ONE ready piece resting on a fixed box; when a hand takes it, it clones a hidden fully-wired TEMPLATE to refill after ~0.6 s → endless supply. Taken pieces get `DispensedConsumable` (self-destruct once picked up then set down idle ~12 s, or below kill-Y) so used strips/sticks don't accumulate. Pure `DispenserMath.IsTaken/ShouldDiscard` pins the policy (`DispenserSuite`, +8). `ReagentCabinetBuilder`: boxes are now `MakeFixedProp` (collider only, NOT grabbable — they stay put); `AttachDispenser` builds template+seed+rest-anchor for Litmus/Matches/Cotton/Filter; new `MakeFilterPaper` (procedural cream disc — user: do it locally like litmus, no Tripo) + `MakeCottonSwab` (stick+two tips); `MakeLitmusStrip/MakeMatchstick` now RETURN the GO and use `AddSinglePhysics` (grabbable, NO DropRespawn — dispenser owns lifecycle). Tripo-swap hooks kept: `FilterPaperSingle`/`CottonSwabSingle`/`LitmusStripSingle`/`MatchstickSingle` in Refs override the procedural piece. Ice stays a bucket-bath (`AddPropPhysics`, unchanged). Rebuild order after cabinet changes: `Build Reagent Cabinets` → `Generate Reagent Labels` → `Wire End-Product Gate` → `Re-Home Scene Items` → suite. *Grab/refill/cleanup FEEL needs a Play-mode pass (dispenser logic is runtime, only the pure math is suite-tested).*

**2026-07-11 education pass:** `LabInfoDatabase` (hover cards) rewritten for depth — every equipment tool now teaches what-it-is + how-to-use-in-procedure + hazard (e.g. burner air collar, meniscus reading, tare-then-weigh), and NEW tokens cover the consumables/apparatus that lacked cards (litmus, matchstick, filterpaper, cottonswab, icebath/icebucket, separatory, florence, thermometer, fumehood). All reagents deepened, and the **11 ready-made 'shortcut' products** (Ethanol/Benzoic Acid/Acetanilide/Acetone/Chloroform/Benzamide/Aspirin/Caffeine/Wine…) now explain how each is really synthesised + its confirmatory test, so hovering a finished product still educates. Raw-reagent catalog blurbs (`RawReagentCatalog.BlurbFor`, with "Used in:" tags) were already solid and left as the fallback (authored `LabInfoDatabase` entries win). Suite unchanged at 769 (titles/tokens/counts preserved). Also: menu-room `music_1.mp3` force-reimported after the user swapped the file under the same name (SoundBank ref is by GUID, so the new audio flows through `music-menu` automatically — same-name swaps need a ForceUpdate reimport to take).

**2026-07-12 W5.10 workspace cleanup (suite 963→969):** (a) **Floating texts gone** — 11 stale Methane labels (`Label_Station_*`, `Label_BeginButton`, `Tag_*`) were authored directly under `WorldLabels` (not under `MethaneStage`), so they floated over the center table forever and the table-merge orphaned them at x≈1.15. Menu `Purge Stale Workspace Labels` (`WorkspaceLabelPurge`) prefix-deletes them; landmark labels `Label_PPELocker`/`Label_FumeHood` kept (WorldLabels 13→2). (b) **Table gantry shelf** — the `Table_1` overhead rails (tops y≈1.55, z=-3.15/-3.50, x[-1.4..1.4]) are now a real equipment shelf: menu `Build Workspace Shelf` (`WorkspaceShelfBuilder` + pure `WorkspaceShelfMath`) removes the user's ad-hoc half-plank and lays 3 flush full-width tiles (top y=1.563 matching the user's plank) + a back-lip, each with a BoxCollider + the bench material; `WorkspaceShelfSuite` pins coverage/height. (c) **Asset tidy** — purged 17 Tripo `*Ref.png` (~17 MB) + the orphan `LitmusBoxRef.mat` OUT of `Assets/` to the gitignored `Docs/raw-art-sources/tripo-refs/` (all were zero-referenced; done via Bash mv, GUID-safe). Fixed a real bug: 3 Tripo prefabs were misnamed vs the code's path-load (`Filter-paperbox`→`FilterPaper`, `LitmusstripeSingle`→`LitmusStripSingle`, `CottonswabSingle`→`CottonSwabSingle`), so the litmus-strip + cotton-swab **single** models were silently falling back to procedural; after renaming + a `Build Reagent Cabinets` rebuild they now show the real Tripo meshes (FilterPaper *single* stays procedural — no such prefab authored). Root library SOs + Tripo prefab locations deliberately NOT moved (path-load breakage). NOTE: on Windows the FS is case-insensitive so a case-only prefab rename needs `AssetDatabase.RenameAsset`, not a Bash mv.

**2026-07-12 W5.9 flow-smoothness + manuscript sweep (suite 920→963):** three deep audits (gatekeeper FSM, lifecycle, manuscript-vs-modules) → ~35 findings fixed. **Flow:** campaign COMPLETION is now celebrated (`PharmeeLines.CampaignComplete` + confetti + grade-pass sting via `AnnounceUnlocks` when `ProgressionFlow.AllComplete()`; Debrief gets a campaign-flavoured remark — it used to promise a nonexistent "next challenge"); grade display FLOORS via `GradeDisplay` (RoundToInt showed "90%" beside TRY AGAIN); `ExperimentRunner.Abort()` ends attempts without grading/recording (HUD `ResetToEntrance` now aborts + hides grade card + `postLab.Close()` — no more zombie runs), `Finish`/`Evaluate` are guarded (no attempt → warn+default; double-finish returns the recorded result); FAIL path gained a **"Choose Another"** grade-screen button (`GateEvent.AbandonRun`: ScoreReview→Blocked, full reset, no debrief; menu `Wire Grade Back Button (W5.9)` cloned+wired it); supply prompt: "Keep trying" `Unlatch(20s)`es the monitor (was an unrecoverable soft dead-end), restart no longer plays the failure outro (`CutsceneDirector.SkipNextOutro`) and teleports inside the Loading fade (`_returnOnLoad`); ambient chatter (PharmeeBrain idle, ExaminerNPC remarks, demo Skip buttons) suppressed during the scripted review via static `PharmeeGatekeeper.ReviewFlowActive` + pure `GatekeeperModel.IsReviewState`; `ScreenFader` callbacks COMPOSE instead of overwrite (overlapping fades used to drop DoLoad and wedge Loading; + 5s Loading watchdog); quiz clock freezes on EVERY path (`PostLabController.Open`), empty quiz bank scores like a missing one (1.0, was 0); door never traps a player inside on Dismiss→ModeChoice; Debrief/spawn lines wait for the fade-in; verb stations re-`Register()` on enable. **Manuscript** (menu `Apply W5.9 Manuscript Data`, idempotent): benzoic ester test was chemically INERT (rule = benzoic+H2SO4, no alcohol) → propyl alcohol staged+bound+rule re-pointed; Chloroform gained the manuscript's missing dichromate+H2SO4 oxidation test (module task `test-oxidation` + reagent + new `Test_ChloroformOxidation` rule); `Chem_GrapeJuice` renamed **"Mixed Fruit Juice"** (manuscript excludes grapes; GUID refs survived, string refs updated); iodoform tests stage their KI; diluted acetic/HCl swaps with RULE INPUTS re-pointed in lockstep; compounding quiz Q3 (bromine/unsaturation misfit) → KMnO4 question. **Flagged to client** (signoff doc + reconciliation §7): Chemical Compounding's whole test battery diverges from manuscript Exp 2 (full redesign — needs sign-off), Safety-mastery coupling, per-attempt history. NOTE `EnsureBinding` gotcha: test-reagent bindings must land on the SEEDED test vessel (the applier prefers same-task → seeded → any, and migrates earlier misplacements).

**2026-07-12 W5.8 experiment-functions overhaul (plan `the-next-line-of-abundant-ripple.md`, suite 808→920):** the interaction layer is now REAL end-to-end. (P1 **pour fix — the big one**) stage vessels used to spawn with a phantom 500 ml default + glass-shell mainRenderer, so pours transferred invisibly and chemistry was dead; now `LiquidPhysics` defaults 0 ml, `SetContents()` seam arms the wake-from-empty branch, `BuildVessel` swaps empty prefabs → `_WithLiquid` twins, `EnsureLiquidVisual` builds a PharmaLiquid child on anything else (props + shelf bottles via `ShelfPourWiring`), and `LiquidPourer.PourTick/ResolveTarget` uses RaycastAll + trigger-ignore + own-hierarchy exclusion (self-tilt can no longer complete pour tasks; station trigger columns no longer swallow streams); `VesselLedger` records the contents story. (P2 **breakage**) `Mishandling.Breakables` = thin glass ONLY (GlassRod/Funnel/Dropper/EvaporatingDish/Crucible delisted, still clink), threshold 4.0→4.5, `DropRespawnMath.ShouldSettleFreeze` re-freezes released bodies in place after ~2.5 s rest (kills solver-spike shatters), `Break()` late-binds/downgrades without a respawn. (P3 **feedback**) `VesselStatus` live tags ("Beaker — 120 ml Ethanol") on every vessel/pourable via existing ProximityLabels; `MixFeedback` popups (`FloatingText`) for reaction observations (`rule.expectedObservation` finally displayed), harmless mixes, "Vessel full!"; hover cards append live "Now: …" lines; `StationStatusLabel` re-uses the station billboard for Heat "62 C -> 150 C" + sim % (THE temp display). (P4 **verbs**) `OrbitMath`+`StirController` (circle the rod in the vessel — works held or on table), `GrindController` (pestle in mortar → powder heap; Methane `prepare-mixture` dual-path via scene mortar, Caffeine stages an educational mortar), `WeighMath`+`WeighStation`+`WeighingScaleController` (Balance fixture + live grams; Aspirin `weigh-salicylic` completes ON THE SCALE — its pour binding is `completesTask:false`; Acetone `weigh-acetates` = scoopula on pan), `Matchstick.ShouldStrike` (swipe held match on `MatchStrikerSurface` — matches box, burner bases, staged striker), `BurnerController` (lit match ignites burner; Heat stations whose prop is a burner GATE their sim on `IsLit` — `ZoneSimStation.SetIgnitionGate` + label hint), methane splint now interactive (`SplintShouldFire`, lit match at the tube, 20 s auto-fallback); `StationSim` += Stir/Grind/Weigh; data via menu `Apply W5.8 Verb Data`. (P5 **table pass**) station pad renderers HIDDEN (colliders/sims/sockets/ghosts intact); `LayoutTidyMath` zoning grid + menu `Tidy Experiment Layouts` re-seated all 10 layouts (stations back row 0.5 m pitch, vessels center-front, reagents right grid, tools left grid — the two (1.38,−3.88) overlaps are structurally gone); every stage now also spawns a `RackKit` (fixed TestTubeRack + 6 grabbable tubes, slot-homes), 3 spare vessels, and (Heat modules) 2 staged matches + striker; `LayoutSpacingSuite` pins deck-bounds + pairwise ≥0.14 m + station pitch. (P6 **polish**) ceiling-magenta re-checked (white — async-shader placeholder confirmed harmless), stale procedures-"tablet" copy → "wrist checklist" (PharmeeLines/PharmeeBrain; quiz-tablet lines kept — that tablet is real), tour prose fixed. **Rebuild order after layout/verb data changes:** `Apply W5.8 Verb Data` → `Tidy Experiment Layouts` → suite. *Pending human Play-mode pass: pour-into-beaker visible fill, stir/grind/weigh feel, match striking + burner ignition, splint pop, settle-freeze feel, rack/spares reachability, popup text sizes.*

**2026-07-12 end-game reagents → shelf-only (suite 769→808):** the finished experiment PRODUCTS now live ONLY on the west-wall `ReagentShelf` (the `Environment/3x4` cubby), hidden in regular lab mode and revealed in a demo session by the existing `EndProductVisibility` gate (user: every end-game reagent belongs on that shelf, never floating by the door). The old floating `SpawnDemoKit` vial (`ExperimentSceneBuilder`, "Ready-made: X (demo)" at world (-0.35,1,0)) is DELETED — it was the thing appearing near the door/glass window. New idempotent builder `EndProductShelfStocker` (menu `Tools ▸ PharmaSynth ▸ Stock End-Product Shelf`) greedily fills the roomiest free cubby slots with the 9 bottleable products; re-run order after it = `Generate Reagent Labels` → `Wire End-Product Gate` → `Wire Shelf Pourers` → `Re-Home Scene Items`. Product-list correction: the 9 gated end products are **Ethanol, Benzoic Acid, Acetanilide, Acetone, Chloroform, Benzamide, Aspirin, Caffeine, Wine** — `DemoMode.IsEndProduct`/`ProductFor` now use **Wine** (new `Chem_Wine` asset, deep burgundy) for winemaking instead of Grape Juice (which is the FEEDSTOCK and stays always-visible, like Sodium Acetate for methane). Methane (a gas) and Chemical Compounding (an identification lab) have no shelf product. Shelf now holds 23 bottles (14 raw + 9 products); DevCapture-verified spacing/labels. Play-mode feel (grab a product in demo, confirm nothing floats by the door in regular) still needs a human pass.

**Spawn/entry (user 2026-07-10):** the game boots into the **cube spawn room** = the `MainMenu` scene rebuilt as a solid, fully-enclosed futuristic cube (cyan emissive trim + glowing launch-pad; `Tools ▸ PharmaSynth ▸ Build Menu Cube Room`, re-runnable). No HUD here. Panel = **Laboratory / Settings / Quit** only (Tutorial dropped — reached in-lab via Pharmee). **Laboratory** fades→loads `SampleScene`, where the HUD appears and **Pharmee greets** (`PharmeeGatekeeper.SpeakWelcome`). **Quit** from the in-lab HUD returns to the cube room; **Reset** from the HUD = `PharmeeGatekeeper.ResetToEntrance` (props re-seated + `PPEController.RemovePPE` + teleport to `FrontDoorSpawn` + gate re-Blocked + Pharmee greets, one fade). Music beds: `MenuMusicPlayer`=`music-menu` (cube) + `LabMusicPlayer`=`music-lab` (lab) — **clips user-supplied later** (SoundBank keys wired). NOTE: `PharmeeGatekeeper.narration`/`ppe` were unwired in the scene and are now hooked (Pharmee's gate lines are now spoken, not just panel text). **Cube polish (pt 2):** the menu rig now has `Move`(DynamicMoveProvider)+`Turn`(Snap+ContinuousTurn) providers (copied from the lab rig) so you walk in the cube; `MenuCubeRoomBuilder` adds neon wall frames + floor grid; `MenuCanvas` restyled to the dialogue-box cyan theme; and `SpawnBurstFX` (scene singleton) + `SpawnVfxBuilder` (`Tools ▸ PharmaSynth ▸ Build Spawn VFX`) play a cyan rising-smoke burst on every spawn/teleport/reset (playOnStart + fired from `TeleportToFrontDoor`).

**2026-07-10 batch (avatar + PPE + lighting + NPC polish):** (a) **Mirror avatar** — Tripo `PlayerAvatar.prefab` (casual clothes) built via `Tools ▸ PharmaSynth ▸ Build Player Avatar`: 1.7 m, foot-offset 0.854 (Tripo pivots at model CENTRE), `PlayerAvatar` layer 8 (main camera culls, mirror renders → mirror-only), Animation-Rigging IK (arms→controllers, head→HMD) via `PlayerAvatarRig`. (b) **Per-piece PPE** — `PPESetModel`+rewritten `PPEController`: coat/goggles/gloves each individually clickable in the locker (`PPEDonOnSelect`), ALL THREE gate the experiment (`PPEWorn`=all; Pharmee names what's missing), worn visuals on the avatar bones (mirror) + first-person gloves on the controllers; `Tools ▸ PharmaSynth ▸ Build PPE Wearables` (idempotent; run AFTER Build Player Avatar). (c) **Lighting** — `Tools ▸ PharmaSynth ▸ Brighten Lab Lighting`: 16 emissive ceiling panels + flat ambient 0.45 + 6 shadowless fill points + key 1.4 (the lab had ONE directional light before). (d) **Pharmee expressions** — `PharmeeFace` multi-renderer via MaterialPropertyBlock on Eyes+Mouth meshes, default HAPPY; `PharmeeMood` (gate-state moods + reset-to-happy on LineEnded); gatekeeper `faceBehaviour` wired. (e) **Jimenez proctor roaming** — `ProctorRoamModel`(pure)+`ProctorRoamer` (walks to 4 `ProctorPoints`, drives "Walking" bool, home from ExperimentFinished→next start). (f) **Station VFX** — `StationVfx` (procedural particles: Heat=steam, Crystallise=frost, Filter=drip, Collect=bubbles) auto-attached by the stage builder, occupancy-driven like SimLoopAudio. (g) **AC hum** — `ProximityHum` on `Environment/AC` (3D rolloff, louder up close, `ambient-lab` clip). NPC/AC wiring menu: `Tools ▸ PharmaSynth ▸ Wire NPC Polish`. **Pt 2:** `SpillPuddle` (spilled/broken vessels leave a chemical-tinted wet disc — 3 s linger + 1.2 s fade; fired from `SpillMistake` + `BreakableGlassware`), Jimenez CharacterController (walls stop his roam; `ProctorRoamer` moves via `cc.Move`), sinks (`WashTable`s) re-centred on their island axes, stools tucked flush to cabinet faces, `Prop_glass-tube` unburied from inside the fume-hood counter, and **Tools ▸ PharmaSynth ▸ Re-Home Scene Items (Adopt Current)** (adopt current transforms as DropRespawn homes — run after ANY manual re-arrangement). **Pt 3:** stools VR-grabbable (kinematic-parked + GrabPhysicsPolicy), spawn burst = 3 waves (t=0/0.4/0.8), mirror-avatar orientation FIX (head bone driven by HMD **delta** onto its authored pose — raw HMD rotation twisted it 180°; arm IK position-only — raw controller rotation twisted wrists), and `PlayFromMenuBootstrap` (`[InitializeOnLoad]`: editor Play ALWAYS boots MainMenu/cube room; toggle Tools ▸ PharmaSynth ▸ Play Starts In Cube Room). **Pt 4 (Jimenez ran through a wall endlessly):** ProctorRoamer never-escape failsafes — 2 s stuck watchdog (blocked walk = arrived; stuck going home = teleport home) + 9 m leash snap-back; CC capsule bounds-fitted to his feet. Suite 545.

**2026-07-10 batch 2 (chemistry + VFX + rich NPC dialogue):** (a) **Confirmatory-test rules completed** — `Test_AcetoneSchiff` (negative/ketone: `Chem_SchiffReagent` + pourable Schiff vial + seeded-tube binding on Layout_Acetone) and `Test_CaffeineMurexide` (positive purple: `Chem_Caffeine` seeded product + `Chem_MurexideReagent` + `Chem_Murexide` result + a new seeded product tube on Layout_Caffeine); registry now **22 rules**, all `FindReaction`-verified, library resolves the 4 new chemicals. (b) **One-shot VFX set** — `EffectVfx` (procedural, shared soft-dot, auto-destroy): glass-shatter shard-burst (`BreakableGlassware.Break`), grade **confetti** (`GradeScreenController.Show` on pass), and a **flame** set — looping burner flame under the steam at Heat stations (built into `StationVfx`) + `FlamePop` on burner-ignite and the methane **splint** flame-test (`MethaneApparatusRig`). (c) **Richer NPC dialogue/behaviour** — `PharmeeLines` (pure, tested pools) gives Pharmee line VARIETY + praise-folded-into-instructions + ambient idle chatter (`PharmeeBrain.Update`, ~16 s quiet); `ExaminerNPC` gained a proctor VOICE (stern greeting + periodic remarks, drives the **"Talking" bool** — `Wire NPC Polish`→`EnsureTalkParam`) **plus a visible world-space `JimenezSubtitles` overhead bubble** (own `NPCNarrationController`, name plate + subtitle, only-while-speaking; built by `Wire NPC Polish`, DevCapture-verified). (d) **Entrance seams sealed** — `Tools ▸ PharmaSynth ▸ Seal Entrance Gaps` (`EntranceSealBuilder`) wraps both doorways with wall-matched trim (DevCapture-verified). (e) Pharmee **hover-wave ×30** (`PharmeeAttitude.waveSpeedMultiplier`). Suite **555**. (f) **Upgraded SFX** (user-approved credit spend) — 7 AI clips via `elevenlabs-sound-effects-v2` in `Audio/Generated/` re-pointed in the SoundBank: real `glass-shatter` + `glass-clink`, 4 Pharmee voice chirps (`pharmee-*`, volumes 0.08→0.3–0.4), and a dedicated `ac-hum` HVAC loop now driving the AC's `ProximityHum` (was `ambient-lab`). **All pending a human listen.** (g) **Audio realism** — per-shot **pitch variation** on physical SFX (`AudioService.JitteredPitch`/`PitchVaries`, UI/musical stings excluded) + a **continuous positional pour sound** (`LiquidPourer` looping 3D source, `PourVolume` curve — pouring was silent before). (h) **Pharmee gives way** — `PharmeeGiveWay` (`SideStep` pure + driver) steps him aside when the player walks into him (0.95 m bubble → lateral push via additive `FloatBob.SetGiveWayOffset`, eased in/out), wired by `Wire NPC Polish`. Suite **562**.

**2026-07-10 batch 3 (perf + VR affordances, smoother-UX goals):** (a) **Per-frame GC removed** — `PharmeeMover` reuses a cached anchor list (was `new List` every frame all run), `ProximityLabel` caches its renderer array (was `GetComponentsInChildren` every frame while shown). (b) **Mirror render-gated on gaze** — `MirrorPlane.ShouldRender` (near + in-front + facing-toward, ~105°) skips the scene's costliest per-frame op when you look away. (c) **Positional 3D SFX** — `AudioService.PlayAt3D` carries drops (`ImpactSound`, at contact) + glass-shatter (`BreakableGlassware`). (d) **Haptics** — `Tools ▸ PharmaSynth ▸ Wire VR Affordances` gives NearFar + Poke interactors `HapticImpulsePlayer`+`SimpleHapticFeedback` (grab/socket/poke buzz). (e) **Hover-highlight** — `HoverHighlight` on all grabbables (58 + spawned) brightens+pops on hover (MPB base-colour, `HighlightScale` tested). (f) **Socket ghost** — station sockets show a translucent-cyan preview of the correct item (`showInteractableHoverMeshes`; `SocketGhost.mat` + runtime mat in the builder). (g) **Wayfinding** verified already-wired (`WaypointGuide`+`WaypointBeacon`→`WaypointMarker`). Suite **568**. *Haptics / ghost render / rolloff feel need an on-device or Play-mode check.*

**2026-07-10 batch 4 (hover learning + audio/pad fixes, teach-as-you-play goals):** (a) **Hover-info panes** — point the right-hand ray (gaze fallback) at any reagent bottle, apparatus or NPC and a smoothly fading/scaling card names it and explains it (`HoverInfoPanel` + `HoverInspector`, `Tools ▸ PharmaSynth ▸ Build Hover Info Panel`). Content from `LabInfoDatabase` (pure, 31 equipment how-to/what-for + 37 reagent trivia/hazard + Pharmee/Jimenez), resolved by chemical name (`LiquidPhysics.currentChemical`), `LabItem`/prop name, or NPC marker; DevCapture-verified card layout. (b) **Hover + grab SFX** — `HoverHighlight` now plays a soft `hover` blip on hover-enter (globally throttled 0.09 s) + the `grab` cue on select, **centralised** so it fires once on EVERY grabbable (removed the dup in `GrabPhysicsPolicy`). New synthesised `hover.wav` (free, no credits). (c) **Footsteps now audible** — the generated `footstep.wav` peaked at **4.3 %** (near-silent, masked); peak-normalised in place to ~85 % (also `glass-clink` 3.2 %→72 %, `grab`→62 %). (d) **Table-pad / holo text fixed** — LiberationSans SDF lacks `☑ ☐ ▶ → Δ ↑` (rendered as blank boxes; the empty `- Fallback.asset` gave nothing), so `GlyphSafe.Sanitize` maps them to font-safe forms at display time (→ `->`, ↑ `(g)`, Δ `(heat)`, ▶ `»`) and the checklist markers became coloured `• » □`; applied in `TabletChecklistController` + `WristWatchController` (pads AND wrist holo). (e) **Watch panel** verified working — gesture (supination + gaze, during an experiment) shows the compact MiniPanel + the holo checklist board; wired the previously-null `summaryText` so the mini panel isn't blank. (f) **Corner music speaker** — `MusicSpeaker` (3D positional source in the back-right corner, `Tools ▸ PharmaSynth ▸ Build Lab Music Speaker`): plays the shuffled `Assets/Background_Music/Lab` playlist (5 tracks, streaming), louder as you approach (log rolloff min 3.5 / max 24), and its volume rides `ScreenFader.State.Alpha` so lab music **fades in on entry / out on exit** (also eases 0→1 from silence on load regardless). Old 2D `LabMusicPlayer` bed disabled; `music-menu` re-pointed to the user's `MenuRoom/music_1`. A procedural bookshelf-speaker mesh sits on the bench (raycast-placed). (g) **Hover-card occlusion fix** — the info card now anchors at the ray-hit surface point and is guaranteed ≥0.12 m in front of it (`HoverInfoPanel.PlaceDistance`), so a close target like Pharmee can't bury it. (h) **Audio/UI polish** — player footsteps lowered (SoundBank 0.55→0.4); `NpcFootsteps` gives Dr. Jimenez subtle 3D footsteps while he roams (own source, vol 0.22, StrideMath); `UiButtonSounds` (hover blip + `ui-click`) auto-added to every Button via `Tools ▸ PharmaSynth ▸ Wire Button Sounds` (run per scene — 21 in SampleScene + MainMenu); and the cube-room `MenuCanvas`+`SettingsPanel` got `FaceCamera` so the menu panel turns to face the player wherever they stand. Suite **610**. *SFX loudness / hover-card feel / watch gesture / music mix / button-hover frequency best judged in a Play-mode or on-device pass.*

**2026-07-10 batch 5 (menu/PPE/spill bug-fixes):** (a) **Cube-room settings Back button** — the SettingsPanel had NO close control and overlaps MenuCanvas (covering the Settings toggle), so the player was trapped and couldn't reach Laboratory; added a styled **Back** button wired to `MainMenuController.OnSettings` (toggles the panel off). (b) **Restart re-seats wearables** — `WearableReseat` (on `PPELocker`) snapshots the locker displays (`LabCoatDisplay`/`Goggles_Standin`/`Gloves_Standin`) at Start and restores them + strips worn PPE; the gatekeeper calls it on **CoatPrompt** entry and in **ResetToEntrance**, so the lab coat is always back on its peg (it had gone missing after a lab-tour→campaign restart, blocking entry). (c) **Pour/spill liquid** — `LiquidPourer` now emits a procedural **tinted droplet stream** from the vessel mouth while tilted past the pour threshold (gravity-driven, colour = the liquid; independent of the optional LineRenderer which was unwired) and drops a **wet puddle where the stream lands** (throttled ~0.6 s) — pouring/spilling was previously invisible. Null-safe `Mouth` fallback (spout→transform). (d) **Wearable hover cards** — `LabInfoDatabase` gained Lab Coat / Safety Goggles / Nitrile Gloves / Casual Clothes entries so pointing at the locker PPE explains each. Suite **613**. *Spill stream/puddle density + the coat-missing repro best confirmed in a Play-mode pass.*

**The confirmed loop (client-approved 2026-07-09):** inside `SampleScene` the player starts in the front corridor. Pharmee guards the **lab door** (the interior door with a window at x≈−2.35, z≈−0.16; the old wall gap is bricked over by `GapSealWall`). Approaching opens his choice panel: **Lab Tour** (door swings open, nothing graded; Pharmee gives a **location-triggered guided tour** — `LabTourGuide` on RobotNPC narrates each landmark as the player reaches it (DynamicStage/EquipmentShelf/ReagentShelf), timed `PharmeeLines.TourBeats` fallback; poke Pharmee to end) or **Campaign** → explain → episode picker (Tutorial/Prelims/Midterms/Finals, locked periods dimmed) → click the **lab coat, goggles AND gloves** in the locker (each individually) to don full PPE → "I'm ready" → fade + stage loads **armed** (clock frozen) → "the period starts as soon as you walk in" → Proceed → door swings open → **crossing the threshold starts the timer**. Pass → Continue on the grade screen → congrats → fade-teleport to `FrontDoorSpawn` → unlock announcement → loop repeats. Reagent bottles are finite (~125 ml ≈ 1 needed pour + 2 spares); starving a required step → Pharmee offers a period restart (failed attempt recorded). Keyboard dev bypass: **B/1-5/F/R** (`DevExperimentDriver`).

**UI:** screen-locked HUD (`HudRig`, frustum-fitted canvas — Progress top-left, timer top-centre, Settings/Restart/Quit top-right, **Pharmee dialogue bar with icon at the bottom**, visible only while he speaks, 0.5 s fade-out). His overhead bubble (name plate + subtitle) shows only while speaking. `ScreenFader` covers scene loads + teleports. In-lab Settings panel = 4 volume sliders; Quit → MainMenu (cube spawn room); Restart = full **reset-to-entrance** (props re-seated + PPE removed + teleport to `FrontDoorSpawn` + Pharmee greets). HUD/bubble/labels outrank transparent geometry via sorting orders (HUD 30000, bubble 29000, world panels 4000–5000, TMP labels 20000).

**Sizes/physics:** all apparatus at real-world scale (`RealSizes` table, 42 prefabs — the ChemLab meshes are metre-accurate; never height-normalise flat tools). `HeadCollisionPushback` on the rig (head-to-KNEE capsule since 2026-07-10 — head-only let players glide through waist-high furniture) stops phasing through static geometry regardless of movement mode (the simulator's WASD moves the HMD and bypasses the CharacterController by design; thumbstick uses the CC). **Physics pass (2026-07-09):** `PhysicsProfiles` (mass + resting pose per prefab, companion to RealSizes), `GrabPhysicsPolicy` (kinematic-on-shelf → dynamic-on-release), `DropRespawn` (kill-Z + idle return-home) — builder applies all three to spawned props (vessels: profile only, stay kinematic). Any item hosting a Rigidbody MUST have convex MeshColliders (PhysX drops concave ones on dynamic bodies → items fall through the world); `PhysicsProfiles.EnsurePhysics` convexifies + pads degenerate flat-tool colliders. Audit menus: **Tools ▸ PharmaSynth ▸ Physics Audit (Report / Fix Scene Items / Drop Test)** — drop test 42/42 green, report clean.

**Audio:** CC0 clips wired (Pharmee beeps at 0.08–0.10, UI/glass/alarm, menu music, grade stings). AI-generated 2026-07-09 (ElevenLabs via Unity credits, in `Audio/Generated/`): gentle `ambient-lab` hum (vol 0.15), `pour`, `bubble` (boil loop), `burner-ignite`, plus new keys `stir` + `footstep` — all wired in SoundBank, pending a human listen. All 13 action-SFX clips wired AND hooked (footsteps/grab/drops/door/PPE/socket/reaction cues — see checklist §4); sim-loop audio still open. Label style locked: MODERN — Tools ▸ PharmaSynth ▸ Generate Reagent Labels (LabelForge) composites TMP names onto the base + mounts quads on the 16 shelf bottles (re-run after chemical changes). Fume hood: generated model at 2.35 m; matte single-sided material + TopFascia/CornerPanel_L/R primitives hide its broken geometry bands. FootstepPlayer tracks the HEAD (simulator WASD moves the HMD only). Builder also spawns per-station: snap socket + SelectSfx, SimLoopAudio (boil/drip/hiss/shimmer while occupied), TeleportationAnchor floor pad, and a player-only >50°C HotSurface HazardZone at Heat stations. MainMenu is XR-ized (full rig active, world-space canvases, XRUIInputModule with mouse kept on) and rebuilt as the futuristic **cube spawn room** (`MenuCubeRoomBuilder`; old `MenuRoom` dressing + `ExperimentSelect` roster + `Results` button hidden; menu = Laboratory/Settings/Quit). New `music-lab` SoundBank key + `LabMusicPlayer` in SampleScene (clip pending).

